/* eslint-disable sort-keys-fix/sort-keys-fix */
import { style } from '@vanilla-extract/css';

export const gradientBox = style({
  position: 'absolute',
  pointerEvents: 'none',
  zIndex: 0,
  inset: 0,
});
