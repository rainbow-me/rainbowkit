import { Tokens } from './tokens';

export type Theme = {
  colors: Tokens['colors'];
  fonts: Tokens['fonts'];
  radii: Tokens['radii'];
  space: Tokens['space'];
  fontSizes: Tokens['fontSizes'];
};
