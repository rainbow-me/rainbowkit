---
title: TombolWallet
description: Menggunakan dan menyesuaikan TombolWallet
---

# TombolWallet

> Catatan: `TombolWallet` saat ini mengandalkan standar dompet EIP-1193, tetapi akan segera mendukung EIP-6963.

Komponen baru `TombolWallet` membantu dApps dengan implementasi daftar dompet kustom mengadopsi RainbowKit dan semua keuntungannya yang bebas perawatan.

```tsx
import { WalletButton } from '@rainbow-me/rainbowkit';

<WalletButton wallet="rainbow" />
<WalletButton wallet="metamask" />
<WalletButton wallet="baseAccount" />
```

Seperti `TombolConnect`, komponen `TombolWallet.Custom` tersedia untuk implementasi dan gaya kustom.

```tsx
<WalletButton.Custom wallet="rainbow">
  {({ ready, connect }) => {
    return (
      <button
        type="button"
        disabled={!ready}
        onClick={connect}
      >
        Connect Rainbow
      </button>
    );
  }}
</WalletButton.Custom>
```

Sebagian besar dApps terbaik dilayani oleh [TombolConnect](https://www.rainbowkit.com/docs/connect-button). Rujuk dokumen [di sini](https://www.rainbowkit.com/docs/wallet-button) untuk informasi lebih lanjut tentang adopsi dan kasus penggunaan `TombolWallet`.

#### Demo

Tekan tombol di bawah ini untuk melihat seperti apa titik masuk RainbowKit ini.

<WalletButtonDemo />
