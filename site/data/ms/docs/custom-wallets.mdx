---
title: Dompet Tersuai
description: Cipta dompet tersuai
---

# Dompet Tersuai

## Cipta dompet tersuai

> Nota: API ini tidak stabil dan kemungkinan akan berubah dalam masa terdekat. Kami akan menambah lebih banyak dompet terbina dalam dari semasa ke semasa. [Beritahu kami](https://github.com/rainbow-me/rainbowkit/issues) jika ada dompet yang khusus anda minati.

Jenis fungsi `Wallet` disediakan untuk membantu anda mentakrifkan dompet tersuai anda sendiri. Sifat-sifat berikut boleh dikonfigurasi pada nilai pulangan fungsi `Wallet` anda:

### Sifat-sifat Dompet

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: 'ID unik setiap dompet',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: 'Nama dompet yang mudah difahami manusia',
    },
    {
      name: 'rdns',
      required: false,
      type: 'string',
      description: 'RDNS untuk dompet yang menyokong EIP6963',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: 'Nama pendek pilihan untuk kegunaan mudah alih',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description:
        'URL untuk ikon dompet, atau janji yang menyelesaikan kepada URL data Base64',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description:
        'Warna aksen digunakan dalam aliran muat turun untuk dompet dengan kedua pelanjutan pelayar dan aplikasi mudah alih',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: 'Warna latar belakang semasa ikon dompet dimuatkan',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          Sama ada dompet diketahui dipasang, atau{' '}
          <code>undefined</code> jika tiada kepastian
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: 'Objek yang mengandungi URL muat turun',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description:
        'Fungsi untuk mengira sama ada dompet ini perlu disembunyikan dari senarai. Ini berguna untuk mentakrif dompet pengganti generik, contohnya pengganti lalai "Injected Wallet" dibina menggunakan ciri ini',
    },
    {
      name: 'createConnector',
      required: true,
      type: '() => RainbowKitConnector',
      typeSimple: 'RainbowKitConnector',
      description:
        'Fungsi untuk menyediakan contoh penyambung dan konfigurasi untuk pelbagai kaedah sambungan, diterangkan di bawah',
    },
    {
      name: 'mobile',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description:
        'Fungsi untuk menyelesaikan URI sambungan dompet mudah alih',
    },
    {
      name: 'desktop',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description: 'Fungsi untuk menyelesaikan penghubung dalam app desktop',
    },
    {
      name: 'qrCode',
      required: false,
      type: "{ getUri: (uri: string) => string, instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Objek yang mengandungi fungsi untuk menyelesaikan URI kod QR, serta arahan persediaan dompet mudah alih pilihan',
    },
    {
      name: 'extension',
      required: false,
      type: "{ instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Objek yang mengandungi arahan persediaan pelanjutan pelayar pilihan',
    },
    {
      name: 'createConnector',
      required: true,
      type: "createConnector: (details: WalletDetailsParams) => CreateConnectorFn",
      typeSimple: '(details: WalletDetailsParams) => CreateConnectorFn',
      description: 'Fungsi untuk menyediakan contoh penyambung',
    },
  ]}
/>

### Sifat-sifat DownloadUrls

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'URL Google Play',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'URL Apple App Store',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        'Halaman pendaratan untuk pengguna mudah alih apabila `android` atau `ios` tidak tersedia',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        'Halaman pendaratan untuk pengguna yang mengimbas kod QR muat turun mudah alih',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'URL Chrome Web Store',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'URL Microsoft Edge Add-ons',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'URL Tambahan Pelayar Firefox',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'URL tambahan Opera',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'URL Mac App Store',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        'Halaman pendaratan untuk pengguna sambungan desktop apabila URL yang serasi pelayar tidak tersedia',
    },
  ]}
/>

### Contoh

Sebagai contoh, untuk mencipta dompet tersuai menggunakan WalletConnect:

```tsx
import { Wallet, getWalletConnectConnector } from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
}

export const rainbow = ({ projectId }: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  mobile: {
    getUri: (uri: string) => uri,
  },
  qrCode: {
    getUri: (uri: string) => uri,
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend putting My Wallet on your home screen for faster access to your wallet.',
          step: 'install',
          title: 'Open the My Wallet app',
        },
        {
          description:
            'After you scan, a connection prompt will appear for you to connect your wallet.',
          step: 'scan',
          title: 'Tap the scan button',
        },
      ],
    },
  },
  extension: {
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
          step: 'install',
          title: 'Install the My Wallet extension',
        },
        {
          description:
            'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
          step: 'create',
          title: 'Create or Import a Wallet',
        },
        {
          description:
            'Once you set up your wallet, click below to refresh the browser and load up the extension.',
          step: 'refresh',
          title: 'Refresh your browser',
        },
      ],
    },
  },
  createConnector: getWalletConnectConnector({ projectId }),
});
```

Anda kemudiannya boleh menambah dompet tersuai anda dalam [senarai dompet](/docs/custom-wallet-list).
