---
title: Крутой режим
description: Включение крутого режима
---

# Крутой режим

## Включение крутого режима 🌈🌈🌈

Вы можете включить крутой режим с помощью свойства `coolMode` в `RainbowKitProvider`.

```tsx
import { RainbowKitProvider } from '@rainbow-me/rainbowkit';

const App = () => (
  <RainbowKitProvider coolMode {...etc}>
    {/* Your App */}
  </RainbowKitProvider>
);
```

### Но что это такое?

Взрывы эмодзи при выборе кошелька из списка кошельков 🌈🌈🌈. Он использует значок выбранного вами кошелька.

#### Демо

Нажмите кнопки ниже, чтобы увидеть, что делает крутой режим.

<CoolMode />
