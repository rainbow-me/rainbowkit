---
title: Крутой режим
description: Включение крутого режима
---

# Крутой режим

## Включение крутого режима 🌈🌈🌈

Вы можете включить крутой режим через свойство `coolMode` в `RainbowKitProvider`.

```tsx
import { RainbowKitProvider } from '@rainbow-me/rainbowkit';

const App = () => (
  <RainbowKitProvider coolMode {...etc}>
    {/* Your App */}
  </RainbowKitProvider>
);
```

### Но что это такое?

Эмоции взрываются, когда выбирается кошелек из списка кошельков 🌈🌈🌈. Используется значок выбранного кошелька.

#### Демо

Нажмите кнопки ниже, чтобы увидеть, что делает крутой режим.

<CoolMode />
