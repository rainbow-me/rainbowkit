---
title: Пользовательские кошельки
description: Создать пользовательский кошелек
---

# Пользовательские кошельки

## Создать пользовательский кошелек

> Примечание: АПИ находится в стадии разработки и может измениться в ближайшем будущем. Со временем мы добавим больше встроенных кошельков. [Сообщите нам](https://github.com/rainbow-me/rainbowkit/issues) если есть конкретные кошельки, которые вас интересуют.

Тип функции `Wallet` предоставляется для помощи в определении ваших собственных пользовательских кошельков. Следующие свойства могут быть настроены в возвращаемом значении вашей функции `Wallet` :

### Свойства Кошелька

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: 'Unique ID per wallet',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: 'Human-readable wallet name',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: 'Optional short name for mobile use',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description:
        'URL для иконки кошелька или обещание, разрешающееся в Base64 data URL',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description:
        'Цвет акцента, используемый в процессах скачивания для кошельков с расширением для браузера и мобильным приложением',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: 'Цвет фона во время загрузки иконки кошелька',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          Установлен ли кошелек, или{' '}
          <code>undefined</code> в случае неполной информации
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: 'Объект с URL-адресами для скачивания',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description:
        'Функция для определения, должен ли этот кошелек быть скрытым из списка. Для определения общих резервных кошельков, таких как встроенный резервный кошелек «Injected Wallet», это полезно',
    },
    {
      name: 'createConnector',
      required: true,
      type: '() => RainbowKitConnector',
      typeSimple: 'RainbowKitConnector',
      description:
        'Функция для предоставления экземпляра коннектора и настройки для различных методов подключения, описанных ниже',
    },
  ]}
/>

### Свойства DownloadUrls

0

### Свойства RainbowKitConnector

Следующие свойства определены для возвращаемого значения функции `createConnector`.

<PropsTable
  data={[
    {
      name: 'mobile',
      required: false,
      type: '{ getUri?: () => Promise<string> }',
      typeSimple: 'object',
      description:
        'Function for resolving a mobile wallet connection URI',
    },
    {
      name: 'desktop',
      required: false,
      type: '{ getUri?: () => Promise<string> }',
      typeSimple: 'object',
      description: 'Function for resolving a desktop app deep link',
    },
    {
      name: 'qrCode',
      required: false,
      type: "{ getUri: () => Promise<string>, instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }}}}",
      typeSimple: 'object',
      description:
        'Object containing a function for resolving the QR code URI, plus optional mobile wallet setup instructions',
    },
    {
      name: 'extension',
      required: false,
      type: "{ instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }}}}",
      typeSimple: 'object',
      description:
        'Object containing optional browser extension setup instructions',
    },
  ]}
/>

### Примеры

Например, для создания пользовательского кошелька с использованием WalletConnect:

```tsx
import {
  Chain,
  Wallet,
  getWalletConnectConnector,
} from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
  chains: Chain[];
}

export const rainbow = ({
  chains,
  projectId,
}: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  createConnector: () => {
    const connector = getWalletConnectConnector({ projectId, chains });

    return {
      connector,
      mobile: {
        getUri: async () => {
          const provider = await connector.getProvider();
          const uri = await new Promise<string>(resolve =>
            provider.once('display_uri', resolve)
          );
          return uri;
        },
      },
      qrCode: {
        getUri: async () => {
          const provider = await connector.getProvider();
          const uri = await new Promise<string>(resolve =>
            provider.once('display_uri', resolve)
          );
          return uri;
        },
        instructions: {
          learnMoreUrl: 'https://my-wallet/learn-more',
          steps: [
            {
              description:
                'We recommend putting My Wallet on your home screen for faster access to your wallet.',
              step: 'install',
              title: 'Open the My Wallet app',
            },
            {
              description:
                'After you scan, a connection prompt will appear for you to connect your wallet.',
              step: 'scan',
              title: 'Tap the scan button',
            },
          ],
        },
      },
      extension: {
        instructions: {
          learnMoreUrl: 'https://my-wallet/learn-more',
          steps: [
            {
              description:
                'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
              step: 'install',
              title: 'Install the My Wallet extension',
            },
            {
              description:
                'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
              step: 'create',
              title: 'Create or Import a Wallet',
            },
            {
              description:
                'Once you set up your wallet, click below to refresh the browser and load up the extension.',
              step: 'refresh',
              title: 'Refresh your browser',
            },
          ],
        },
      },
    };
  },
});
```

Затем вы можете добавить свой пользовательский кошелек в [список кошельков](/docs/custom-wallet-list).
