---
title: 自定義 ConnectButton
description: 創建自定義 ConnectButton
---

# 自定義 ConnectButton

## 創建自定義 ConnectButton

您可以使用低階的 `ConnectButton.Custom` 來建立自己的自定義連接按鈕。此元件呈現一個函數，其中包含您重新實現內建按鈕所需的一切。

內建按鈕的最小重實現看起來大概是這樣的：

```tsx
import { ConnectButton } from '@rainbow-me/rainbowkit';

export const YourApp = () => {
  return (
    <ConnectButton.Custom>
      {({
        account,
        chain,
        openAccountModal,
        openChainModal,
        openConnectModal,
        authenticationStatus,
        mounted,
      }) => {
        // Note: If your app doesn't use authentication, you
        // can remove all 'authenticationStatus' checks
        const ready = mounted && authenticationStatus !== 'loading';
        const connected =
          ready &&
          account &&
          chain &&
          (!authenticationStatus ||
            authenticationStatus === 'authenticated');

        return (
          <div
            {...(!ready && {
              'aria-hidden': true,
              'style': {
                opacity: 0,
                pointerEvents: 'none',
                userSelect: 'none',
              },
            })}
          >
            {(() => {
              if (!connected) {
                return (
                  <button onClick={openConnectModal} type="button">
                    Connect Wallet
                  </button>
                );
              }

              if (chain.unsupported) {
                return (
                  <button onClick={openChainModal} type="button">
                    Wrong network
                  </button>
                );
              }

              return (
                <div style={{ display: 'flex', gap: 12 }}>
                  <button
                    onClick={openChainModal}
                    style={{ display: 'flex', alignItems: 'center' }}
                    type="button"
                  >
                    {chain.hasIcon && (
                      <div
                        style={{
                          background: chain.iconBackground,
                          width: 12,
                          height: 12,
                          borderRadius: 999,
                          overflow: 'hidden',
                          marginRight: 4,
                        }}
                      >
                        {chain.iconUrl && (
                          <img
                            alt={chain.name ?? 'Chain icon'}
                            src={chain.iconUrl}
                            style={{ width: 12, height: 12 }}
                          />
                        )}
                      </div>
                    )}
                    {chain.name}
                  </button>

                  <button onClick={openAccountModal} type="button">
                    {account.displayName}
                    {account.displayBalance
                      ? ` (${account.displayBalance})`
                      : ''}
                  </button>
                </div>
              );
            })()}
          </div>
        );
      }}
    </ConnectButton.Custom>
  );
};
```

以下屬性將傳遞給您的渲染函數。

### 帳戶屬性

<PropsTable
  data={[
    {
      name: 'account',
      required: false,
      type: 'object | undefined',
      description:
        '包含當前帳戶詳情的物件，詳述如下',
    },
    {
      name: 'account.address',
      required: false,
      type: 'string',
      description: (
        <>
          完整的帳戶地址，例如
          "0x7a3d05c70581bD345fe117c06e45f9669205384f"
        </>
      ),
    },
    {
      name: 'account.balanceDecimals',
      required: false,
      type: 'string | undefined	',
      description: '帳戶餘額的小數',
    },
    {
      name: 'account.balanceFormatted',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          帳戶餘額格式化為字串，例如{' '}
          <code>1.234567890123456789</code>
        </>
      ),
    },
    {
      name: 'account.balanceSymbol',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          餘額的貨幣符號，例如 <code>ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayBalance',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          格式化為 3 個有效數字的餘額，加上
          符號，例如 <code>1.23 ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayName',
      required: false,
      type: 'string',
      description: (
        <>
          ENS 名稱，或精簡版的地址，例如{' '}
          <code>"rainbowwallet.eth"</code> 或 <code>"0x7a…384f"</code>
        </>
      ),
    },
    {
      name: 'account.ensAvatar',
      required: false,
      type: 'string	| undefined',
      description: 'ENS avatar URI',
    },
    {
      name: 'account.ensName',
      required: false,
      type: 'string	| undefined',
      description: (
        <>
          ENS 名稱，例如 <code>rainbowwallet.eth</code>
        </>
      ),
    },
    {
      name: 'account.hasPendingTransactions',
      required: false,
      type: 'boolean',
      description:
        '布林值，表示當前鏈上的帳戶是否有待定交易',
    },
  ]}
/>

### 鏈屬性

<PropsTable
  data={[
    {
      name: 'chain',
      required: false,
      type: 'object | undefined',
      description:
        '包含當前鏈詳情的物件，詳述如下',
    },
    {
      name: 'chain.hasIcon',
      required: false,
      type: 'boolean',
      description: '鏈是否指定了圖示',
    },
    {
      name: 'chain.iconUrl',
      required: false,
      type: 'string | undefined',
      description:
        '鏈圖示的 URL （在下載 Base64 數據 URL 時可能也為未定義）',
    },
    {
      name: 'chain.iconBackground',
      required: false,
      type: 'string | undefined',
      description:
        '當圖像加載時可見的鏈圖示背景',
    },
    {
      name: 'chain.id',
      required: false,
      type: 'number',
      description: (
        <>
          鏈 ID，例如 <code>1</code>
        </>
      ),
    },
    {
      name: 'chain.name',
      required: false,
      type: 'string | undefined',
      description: (
        <>
          鏈名，例如 <code>"Ethereum"</code>
        </>
      ),
    },
    {
      name: 'chain.unsupported',
      required: false,
      type: 'boolean | undefined',
      description:
        '布林值，指示當前鏈是否不支援',
    },
  ]}
/>

### 模態狀態屬性

<PropsTable
  data={[
    {
      name: 'openAccountModal',
      required: false,
      type: '() => void',
      description: '打開帳戶模態的函數',
    },
    {
      name: 'openChainModal',
      required: false,
      type: '() => void',
      description: '打開鏈模態的函數',
    },
    {
      name: 'openConnectModal',
      required: false,
      type: '() => void',
      description: '打開連接模態的函數',
    },
    {
      name: 'accountModalOpen',
      required: false,
      type: 'boolean',
      description:
        '布林值，指示帳戶模態是否已打開',
    },
    {
      name: 'chainModalOpen',
      required: false,
      type: 'boolean',
      description: '布林值，指示鏈模態是否已打開',
    },
    {
      name: 'connectModalOpen',
      required: false,
      type: 'boolean',
      description:
        '布林值，指示連接模態是否已打開',
    },
  ]}
/>

### 一般狀態屬性

<PropsTable
  data={[
    {
      name: 'mounted',
      required: false,
      type: 'boolean',
      description:
        '布林值，指示元件是否已載入',
    },
    {
      name: 'authenticationStatus',
      required: false,
      type: '"loading" | "unauthenticated" | "authenticated" | undefined',
      description:
        '驗證狀態，或如果尚未配置驗證則為未定義',
    },
  ]}
/>
