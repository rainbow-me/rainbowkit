---
title: カスタムウォレットリスト
description: ウォレットリストのカスタマイズ
---

# カスタムウォレットリスト

## ウォレットリストのカスタマイズ

必要なコネクタを持つウォレットのリストを構築するために、`'@rainbow-me/rainbowkit/wallets'` から個別のウォレットと `connectorsForWallets` 関数をインポートできます。この方法で、どのウォレットをどの順番で表示するかを完全に制御できます。

例えば、Rainbow と一般的なフォールバックウォレットのみを表示することができます。

```tsx
import { connectorsForWallets } from '@rainbow-me/rainbowkit';
import {
  rainbowWallet,
  walletConnectWallet,
} from '@rainbow-me/rainbowkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, walletConnectWallet],
    },
  ],
  {
    appName: 'My RainbowKit App',
    projectId: 'YOUR_PROJECT_ID',
  }
);
```

その後、Wagmi の `createConfig` にコネクタを渡すことができます。

```tsx
import { connectorsForWallets } from '@rainbow-me/rainbowkit';
import { createConfig } from 'wagmi';

const connectors = connectorsForWallets(/* ... */);

const config = createConfig({
  connectors,
  {/* Wagmi config */}
});

const queryClient = new QueryClient();

const App = () => (
  <WagmiProvider config={config}>
    <QueryClientProvider client={queryClient}>
      <RainbowKitProvider {...etc}>
        {/* Your App */}
      </RainbowKitProvider>
    </QueryClientProvider>
  </WagmiProvider>
);
```

### 内蔵ウォレット

以下のウォレットは `wallet` オブジェクトを介して提供されます。

#### 一般ウォレット

以下のウォレットは特定のアプリではなく、一般的な接続方法に対応しています。そのため、これらのウォレットは常に含めることをお勧めします。

##### ウォレットコネクト

これは、他のウォレットリストに提供されていないウォレットコネクトベースのウォレットのために設計されたフォールバックウォレットオプションです。

**ウォレットコネクトベースのすべてのウォレットがサポートされるように、常にこのウォレットをリストに含めることをお勧めします。**

```tsx
import { walletConnectWallet } from '@rainbow-me/rainbowkit/wallets';
```

##### インジェクテッドウォレット

これは、`window.ethereum` が存在するが他のウォレットリストに提供されていないシナリオのために設計されたフォールバックウォレットオプションです。このウォレットは、フォールバックが必要でないか、インジェクテッドウォレットが利用できない場合にはリストから自動的に非表示になります。

**すべてのインジェクテッドウォレットがサポートされるように、常にこのウォレットをリストに含めることをお勧めします。**

```tsx
import { injectedWallet } from '@rainbow-me/rainbowkit/wallets';
```

##### セーフウォレット

SafeアプリをサポートするdAppsには、ユーザーがSafeボールトやマルチシグに簡単に接続できるように`safeWallet`オプションを含めてください。SafeオプションはSafeウォレットブラウザ環境にのみ表示されます。

```tsx
import { safeWallet } from '@rainbow-me/rainbowkit/wallets';
```

### 特定のウォレット

以下のウォレットは（アルファベット順に）`wallet` オブジェクトを介して提供されます。

#### 1inchウォレット

```tsx
import { oneInchWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Backpack

```tsx
import { backpackWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### ベースアカウント（旧称: Coinbase Wallet）

このウォレットコネクタは、Coinbase Smart Walletの進化形であるベースアカウントをサポートします。シームレスな認証と、サブアカウントや支出許可といった強化された機能を提供します。

設定オプションは[ベースアカウントSDKリファレンス](https://docs.base.org/base-account/reference/core/createBaseAccount)と一致します。

```tsx
import { baseAccount } from '@rainbow-me/rainbowkit/wallets';

// Configure attribution (choose one approach)
baseAccount.preference = {
  attribution: { auto: true }, // Smart Wallet generates origin-based identifier
};

// Or use custom attribution
baseAccount.preference = {
  attribution: { dataSuffix: '0x1234567890abcdef' }, // 16-byte hex identifier
};

// Configure sub-accounts
baseAccount.subAccounts = {
  creation: 'on-connect', // or 'manual'
  defaultAccount: 'sub', // or 'universal'
  funding: 'spend-permissions', // or 'manual'
};

// Configure paymaster URLs for gasless transactions
baseAccount.paymasterUrls = {
  [base.id]: 'https://api.developer.coinbase.com/rpc/v1/base/...',
};
```

#### BeraSig

```tsx
import { berasigWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### ベストウォレット

```tsx
import { bestWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Bifrost Wallet

```tsx
import { bifrostWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### バイナンスWeb3ウォレット

```tsx
import { binanceWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Bitget

```tsx
import { bitgetWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Bitski

```tsx
import { bitskiWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Bitverse

```tsx
import { bitverseWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Bloomウォレット

```tsx
import { bloomWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Braveウォレット

```tsx
import { braveWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Bybitウォレット

```tsx
import { bybitWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Coin98ウォレット

```tsx
import { coin98Wallet } from '@rainbow-me/rainbowkit/wallets';
```

#### コンパスウォレット

```tsx
import { compassWallet } from '@rainbow-me/rainbowkit/compassWallet';
```

#### コア

```tsx
import { coreWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### CTRLウォレット

```tsx
import { ctrlWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Dawnウォレット

```tsx
import { dawnWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Enkryptウォレット

```tsx
import { enkryptWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### FoxWallet

```tsx
import { foxWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Frameウォレット

```tsx
import { frameWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Frontierウォレット

```tsx
import { frontierWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Gateウォレット

```tsx
import { gateWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### ジェミニウォレット

このスマートウォレットコネクターは、keys.gemini.comでパスキーを通じたシームレスな認証を可能にします。ダウンロード、拡張機能、APIキーは不要です。そのまま機能します。

`appName` パラメーターはdAppを識別するために必須であり、`appIcon` は接続体験をブランディングするために任意です。

```tsx
import { geminiWallet } from '@rainbow-me/rainbowkit/wallets';

const gemini = geminiWallet({
  appName: 'My App',
  appIcon: 'https://my-app.com/logo.png', // Optional
});
```

#### imToken

```tsx
import { imTokenWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### ioPayウォレット

```tsx
import { iopayWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Kaiaウォレット

```tsx
import { kaiaWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Kaikas

```tsx
import { kaikasWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Krakenウォレット

```tsx
import { krakenWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Kresusウォレット

```tsx
import { kresusWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Ledger Live

```tsx
import { ledgerWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Magic Edenウォレット

```tsx
import { magicEdenWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### MetaMask

```tsx
import { metaMaskWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### MEWウォレット

```tsx
import { mewWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Novaウォレット

```tsx
import { novaWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### OKXウォレット

```tsx
import { okxWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### オムニ

```tsx
import { omniWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### OneKey

```tsx
import { oneKeyWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### ParaSwap

```tsx
import { paraSwapWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Phantom

```tsx
import { phantomWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### ポルト

```tsx
import { portoWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Rabbyウォレット

```tsx
import { rabbyWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### レインボー

```tsx
import { rainbowWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Ramperウォレット

```tsx
import { ramperWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### 準備完了

```tsx
import { readyWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Roninウォレット

```tsx
import { roninWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Safeheron

```tsx
import { safeheronWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Taho

```tsx
import { tahoWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Talisman

```tsx
import { talismanWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Tokenary

```tsx
import { tokenaryWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### TokenPocket

```tsx
import { tokenPocketWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Trust Wallet

```tsx
import { trustWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Uniswapウォレット

```tsx
import { uniswapWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Valora

```tsx
import { valoraWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Wigwam

```tsx
import { wigwamWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### xPortal

```tsx
import { xPortalWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### Zerion

```tsx
import { zerionWallet } from '@rainbow-me/rainbowkit/wallets';
```

#### ZilPay

```tsx
import { zilPayWallet } from '@rainbow-me/rainbowkit/wallets';
```

### 例

#### 順序

異なるウォレットを異なる順序で表示する例をいくつか示します。

MetaMask と一般的なフォールバックウォレットを表示します。

```tsx
import { connectorsForWallets } from '@rainbow-me/rainbowkit';
import {
  metaMaskWallet,
  walletConnectWallet,
} from '@rainbow-me/rainbowkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [metaMaskWallet, walletConnectWallet],
    },
  ],
  { appName: 'RainbowKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

Rainbow、MetaMask、Baseアカウントおよび一般的なフォールバックウォレットを表示します。

```tsx
import { connectorsForWallets } from '@rainbow-me/rainbowkit';
import {
  rainbowWallet,
  metaMaskWallet,
  baseAccount,
  walletConnectWallet,
} from '@rainbow-me/rainbowkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Suggested',
      wallets: [
        rainbowWallet,
        metaMaskWallet,
        baseAccount,
        walletConnectWallet,
      ],
    },
  ],
  { appName: 'RainbowKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

> 注：`wallets` 配列の順序は、UI に表示されるウォレットの順序を決定します。

#### 複数のグループ

`groupName` キーを使用して、異なるウォレットグループに名前を付けることができます。これにより、ユーザーに推奨するウォレットや他の可能なウォレットを伝えることができます。

RainbowとMetaMaskを推奨しますが、Baseアカウントと一般的なフォールバックウォレットも提供します。

```tsx
import { connectorsForWallets } from '@rainbow-me/rainbowkit';
import {
  rainbowWallet,
  metaMaskWallet,
  baseAccount,
  walletConnectWallet,
} from '@rainbow-me/rainbowkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, metaMaskWallet],
    },
    {
      groupName: 'Others',
      wallets: [baseAccount, walletConnectWallet],
    },
  ],
  { appName: 'RainbowKit App', projectId: 'YOUR_PROJECT_ID' },
);
```
