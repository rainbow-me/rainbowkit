---
title: زر اتصال مخصص
description: إنشاء زر اتصال مخصص
---

# زر اتصال مخصص

## إنشاء زر اتصال مخصص

يمكنك استخدام المكون منخفض المستوى `ConnectButton.Custom` لإنشاء زر الاتصال المخصص الخاص بك. يعرض هذا المكون دالة، والتي تتضمن كل ما تحتاجه لإعادة تنفيذ الأزرار المدمجة.

ستبدو إعادة تنفيذ بسيطة للأزرار المدمجة كما يلي:

```tsx
import { ConnectButton } from '@rainbow-me/rainbowkit';

export const YourApp = () => {
  return (
    <ConnectButton.Custom>
      {({
        account,
        chain,
        openAccountModal,
        openChainModal,
        openConnectModal,
        authenticationStatus,
        mounted,
      }) => {
        // Note: If your app doesn't use authentication, you
        // can remove all 'authenticationStatus' checks
        const ready = mounted && authenticationStatus !== 'loading';
        const connected =
          ready &&
          account &&
          chain &&
          (!authenticationStatus ||
            authenticationStatus === 'authenticated');

        return (
          <div
            {...(!ready && {
              'aria-hidden': true,
              'style': {
                opacity: 0,
                pointerEvents: 'none',
                userSelect: 'none',
              },
            })}
          >
            {(() => {
              if (!connected) {
                return (
                  <button onClick={openConnectModal} type="button">
                    Connect Wallet
                  </button>
                );
              }

              if (chain.unsupported) {
                return (
                  <button onClick={openChainModal} type="button">
                    Wrong network
                  </button>
                );
              }

              return (
                <div style={{ display: 'flex', gap: 12 }}>
                  <button
                    onClick={openChainModal}
                    style={{ display: 'flex', alignItems: 'center' }}
                    type="button"
                  >
                    {chain.hasIcon && (
                      <div
                        style={{
                          background: chain.iconBackground,
                          width: 12,
                          height: 12,
                          borderRadius: 999,
                          overflow: 'hidden',
                          marginRight: 4,
                        }}
                      >
                        {chain.iconUrl && (
                          <img
                            alt={chain.name ?? 'Chain icon'}
                            src={chain.iconUrl}
                            style={{ width: 12, height: 12 }}
                          />
                        )}
                      </div>
                    )}
                    {chain.name}
                  </button>

                  <button onClick={openAccountModal} type="button">
                    {account.displayName}
                    {account.displayBalance
                      ? ` (${account.displayBalance})`
                      : ''}
                  </button>
                </div>
              );
            })()}
          </div>
        );
      }}
    </ConnectButton.Custom>
  );
};
```

الخصائص التالية تمرر إلى دالة العرض الخاصة بك.

### خصائص الحساب

<PropsTable
  data={[
    {
      name: 'account',
      required: false,
      type: 'object | undefined',
      description:
        'كائن يحتوي على تفاصيل الحساب الحالي موصوفة أدناه',
    },
    {
      name: 'account.address',
      required: false,
      type: 'string',
      description: (
        <>
          العنوان الكامل للحساب، على سبيل المثال
          "0x7a3d05c70581bD345fe117c06e45f9669205384f"
        </>
      ),
    },
    {
      name: 'account.balanceDecimals',
      required: false,
      type: 'string | undefined	',
      description: 'رصيد الحساب بالأرقام العشرية',
    },
    {
      name: 'account.balanceFormatted',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          الرصيد الحسابي بصيغة نصية، مثل{' '}
          <code>1.234567890123456789</code>
        </>
      ),
    },
    {
      name: 'account.balanceSymbol',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          رمز العملة للرصيد، مثل <code>ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayBalance',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          الرصيد المعروض للعرض الى 3 أرقام مؤثرة، بالإضافة إلى
          الرمز, على سبيل المثال <code>1.23 ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayName',
      required: false,
      type: 'string',
      description: (
        <>
          اسم ENS، أو نسخة مختصرة من العنوان، مثل{' '}
          <code>"rainbowwallet.eth"</code> أو <code>"0x7a…384f"</code>
        </>
      ),
    },
    {
      name: 'account.ensAvatar',
      required: false,
      type: 'string	| undefined',
      description: 'الرابط الذي يؤدي إلى صورة الحساب ENS',
    },
    {
      name: 'account.ensName',
      required: false,
      type: 'string	| undefined',
      description: (
        <>
          اسم ENS، مثل <code>rainbowwallet.eth</code>
        </>
      ),
    },
    {
      name: 'account.hasPendingTransactions',
      required: false,
      type: 'boolean',
      description:
        'علم يحدد ما إذا كان للحساب معاملات معلقة للسلسلة الحالية',
    },
  ]}
/>

### خصائص السلسلة

<PropsTable
  data={[
    {
      name: 'chain',
      required: false,
      type: 'object | undefined',
      description:
        'الكائن الذي يحتوي على تفاصيل السلسلة الحالية موصوفة أدناه',
    },
    {
      name: 'chain.hasIcon',
      required: false,
      type: 'boolean',
      description: 'تحديد ما إذا كانت السلسلة تحتوي على أيقونة محددة',
    },
    {
      name: 'chain.iconUrl',
      required: false,
      type: 'string | undefined',
      description:
        'رابط أيقونة السلسلة (والتي قد تكون undefined أثناء تحميل بيانات Base64)',
    },
    {
      name: 'chain.iconBackground',
      required: false,
      type: 'string | undefined',
      description:
        'خلفية أيقونة السلسلة التي ستكون ظاهرة خلال تحميل الصور',
    },
    {
      name: 'chain.id',
      required: false,
      type: 'number',
      description: (
        <>
          معرف السلسلة، على سبيل المثال <code>1</code>
        </>
      ),
    },
    {
      name: 'chain.name',
      required: false,
      type: 'string | undefined',
      description: (
        <>
          اسم السلسلة، مثل <code>"Ethereum"</code>
        </>
      ),
    },
    {
      name: 'chain.unsupported',
      required: false,
      type: 'boolean | undefined',
      description:
        'علم يحدد ما إذا كانت السلسلة الحالية غير مدعومة',
    },
  ]}
/>

### خصائص حالة النافذة

<PropsTable
  data={[
    {
      name: 'openAccountModal',
      required: false,
      type: '() => void',
      description: 'دالة لفتح نافذة حساب',
    },
    {
      name: 'openChainModal',
      required: false,
      type: '() => void',
      description: 'دالة لفتح نافذة السلسلة',
    },
    {
      name: 'openConnectModal',
      required: false,
      type: '() => void',
      description: 'دالة لفتح نافذة اتصال',
    },
    {
      name: 'accountModalOpen',
      required: false,
      type: 'boolean',
      description:
        'علم يحدد ما إذا كانت نافذة الحساب مفتوحة',
    },
    {
      name: 'chainModalOpen',
      required: false,
      type: 'boolean',
      description: 'علم يحدد ما إذا كانت نافذة السلسلة مفتوحة',
    },
    {
      name: 'connectModalOpen',
      required: false,
      type: 'boolean',
      description:
        'علم يحدد ما إذا كانت نافذة الاتصال مفتوحة',
    },
  ]}
/>

### خصائص حالة العامة

<PropsTable
  data={[
    {
      name: 'mounted',
      required: false,
      type: 'boolean',
      description:
        'علم يحدد ما إذا كان المكون تم تحميله',
    },
    {
      name: 'authenticationStatus',
      required: false,
      type: '"loading" | "unauthenticated" | "authenticated" | undefined',
      description:
        'حالة المصادقة، أو undefined إذا لم يتم تكوين المصادقة',
    },
  ]}
/>
