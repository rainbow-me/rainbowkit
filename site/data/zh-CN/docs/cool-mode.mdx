---
title: 酷模式
description: 启用酷模式
---

# 酷模式

## 启用酷模式 🌈🌈🌈

您可以通过 `RainbowKitProvider` 上的 `coolMode` 属性启用酷模式。

```tsx
import { RainbowKitProvider } from '@rainbow-me/rainbowkit';

const App = () => (
  <RainbowKitProvider coolMode {...etc}>
    {/* Your App */}
  </RainbowKitProvider>
);
```

### 但这是什么？

选择钱包列表中的某个钱包时会产生表情符号爆炸 🌈🌈🌈。 它使用您选择的钱包的图标。

#### 演示

按下面的按钮查看酷模式的作用。

<CoolMode />
