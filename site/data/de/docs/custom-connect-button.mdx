---
title: Benutzerdefinierte ConnectButton
description: Einen benutzerdefinierten ConnectButton erstellen
---

# Benutzerdefinierte ConnectButton

## Einen benutzerdefinierten ConnectButton erstellen

Sie können den Low-Level `ConnectButton.Custom` verwenden, um Ihren eigenen benutzerdefinierten Verbindungsknopf zu erstellen. Diese Komponente rendert eine Funktion, die alles enthält, was Sie benötigen, um die integrierten Schaltflächen neu zu implementieren.

Eine minimale Re-Implementierung der integrierten Schaltflächen könnte so aussehen:

```tsx
import { ConnectButton } from '@rainbow-me/rainbowkit';

export const YourApp = () => {
  return (
    <ConnectButton.Custom>
      {({
        account,
        chain,
        openAccountModal,
        openChainModal,
        openConnectModal,
        authenticationStatus,
        mounted,
      }) => {
        // Note: If your app doesn't use authentication, you
        // can remove all 'authenticationStatus' checks
        const ready = mounted && authenticationStatus !== 'loading';
        const connected =
          ready &&
          account &&
          chain &&
          (!authenticationStatus ||
            authenticationStatus === 'authenticated');

        return (
          <div
            {...(!ready && {
              'aria-hidden': true,
              'style': {
                opacity: 0,
                pointerEvents: 'none',
                userSelect: 'none',
              },
            })}
          >
            {(() => {
              if (!connected) {
                return (
                  <button onClick={openConnectModal} type="button">
                    Connect Wallet
                  </button>
                );
              }

              if (chain.unsupported) {
                return (
                  <button onClick={openChainModal} type="button">
                    Wrong network
                  </button>
                );
              }

              return (
                <div style={{ display: 'flex', gap: 12 }}>
                  <button
                    onClick={openChainModal}
                    style={{ display: 'flex', alignItems: 'center' }}
                    type="button"
                  >
                    {chain.hasIcon && (
                      <div
                        style={{
                          background: chain.iconBackground,
                          width: 12,
                          height: 12,
                          borderRadius: 999,
                          overflow: 'hidden',
                          marginRight: 4,
                        }}
                      >
                        {chain.iconUrl && (
                          <img
                            alt={chain.name ?? 'Chain icon'}
                            src={chain.iconUrl}
                            style={{ width: 12, height: 12 }}
                          />
                        )}
                      </div>
                    )}
                    {chain.name}
                  </button>

                  <button onClick={openAccountModal} type="button">
                    {account.displayName}
                    {account.displayBalance
                      ? ` (${account.displayBalance})`
                      : ''}
                  </button>
                </div>
              );
            })()}
          </div>
        );
      }}
    </ConnectButton.Custom>
  );
};
```

Die folgenden Props werden an Ihre Renderfunktion übergeben.

### Kontoeigenschaften

<PropsTable
  data={[
    {
      name: 'account',
      required: false,
      type: 'object | undefined',
      description:
        'Objekt mit Details zum aktuellen Konto, wie unten beschrieben',
    },
    {
      name: 'account.address',
      required: false,
      type: 'string',
      description: (
        <>
          Die vollständige Kontoadresse, z.B.
          "0x7a3d05c70581bD345fe117c06e45f9669205384f"
        </>
      ),
    },
    {
      name: 'account.balanceDecimals',
      required: false,
      type: 'string | undefined	',
      description: 'Das Kontoguthaben in Dezimalzahlen',
    },
    {
      name: 'account.balanceFormatted',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          Das Kontoguthaben als Zeichenkette formatiert, z. B.{' '}
          <code>1.234567890123456789</code>
        </>
      ),
    },
    {
      name: 'account.balanceSymbol',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          Das Währungssymbol für das Guthaben, z. B. <code>ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayBalance',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          Das Guthaben auf 3 signifikante Stellen gerundet, plus das
          Symbol, z. B. <code>1.23 ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayName',
      required: false,
      type: 'string',
      description: (
        <>
          Der ENS-Name oder eine gekürzte Version der Adresse, z. B.{' '}
          <code>"rainbowwallet.eth"</code> oder <code>"0x7a…384f"</code>
        </>
      ),
    },
    {
      name: 'account.ensAvatar',
      required: false,
      type: 'string	| undefined',
      description: 'Die ENS-Avatar-URI',
    },
    {
      name: 'account.ensName',
      required: false,
      type: 'string	| undefined',
      description: (
        <>
          Der ENS-Name, z. B. <code>rainbowwallet.eth</code>
        </>
      ),
    },
    {
      name: 'account.hasPendingTransactions',
      required: false,
      type: 'boolean',
      description:
        'Boolean, das anzeigt, ob für das aktuelle Netzwerk ausstehende Transaktionen vorhanden sind',
    },
  ]}
/>

### Netzwerkeigenschaften

<PropsTable
  data={[
    {
      name: 'chain',
      required: false,
      type: 'object | undefined',
      description:
        'Objekt mit Details zum aktuellen Netzwerk, wie unten beschrieben',
    },
    {
      name: 'chain.hasIcon',
      required: false,
      type: 'boolean',
      description: 'Ob das Netzwerk ein angegebener Icon hat',
    },
    {
      name: 'chain.iconUrl',
      required: false,
      type: 'string | undefined',
      description:
        'Die URL des Netzwerk-Icons (kann auch undefiniert sein, während Base64-Daten-URLs heruntergeladen werden)',
    },
    {
      name: 'chain.iconBackground',
      required: false,
      type: 'string | undefined',
      description:
        'Der Hintergrund des Netzwerk-Icons, der sichtbar ist, während Bilder geladen werden',
    },
    {
      name: 'chain.id',
      required: false,
      type: 'number',
      description: (
        <>
          Die Netzwerk-ID, z. B. <code>1</code>
        </>
      ),
    },
    {
      name: 'chain.name',
      required: false,
      type: 'string | undefined',
      description: (
        <>
          Der Netzwerk-Name, z. B. <code>"Ethereum"</code>
        </>
      ),
    },
    {
      name: 'chain.unsupported',
      required: false,
      type: 'boolean | undefined',
      description:
        'Boolean, das anzeigt, ob das aktuelle Netzwerk nicht unterstützt wird',
    },
  ]}
/>

### Modal-Zustandseigenschaften

<PropsTable
  data={[
    {
      name: 'openAccountModal',
      required: false,
      type: '() => void',
      description: 'Funktion, um das Konto-Modul zu öffnen',
    },
    {
      name: 'openChainModal',
      required: false,
      type: '() => void',
      description: 'Funktion, um das Netzwerk-Modul zu öffnen',
    },
    {
      name: 'openConnectModal',
      required: false,
      type: '() => void',
      description: 'Funktion, um das Connect-Modul zu öffnen',
    },
    {
      name: 'accountModalOpen',
      required: false,
      type: 'boolean',
      description:
        'Boolean, das anzeigt, ob das Konto-Modul offen ist',
    },
    {
      name: 'chainModalOpen',
      required: false,
      type: 'boolean',
      description: 'Boolean, das anzeigt, ob das Netzwerk-Modul offen ist',
    },
    {
      name: 'connectModalOpen',
      required: false,
      type: 'boolean',
      description:
        'Boolean, das anzeigt, ob das Connect-Modul offen ist',
    },
  ]}
/>

### Allgemeine Zustandseigenschaften

<PropsTable
  data={[
    {
      name: 'mounted',
      required: false,
      type: 'boolean',
      description:
        'Boolean, das anzeigt, ob die Komponente geladen ist',
    },
    {
      name: 'authenticationStatus',
      required: false,
      type: '"loading" | "unauthenticated" | "authenticated" | undefined',
      description:
        'Der Authentifizierungsstatus oder undefined, wenn die Authentifizierung nicht konfiguriert wurde',
    },
  ]}
/>
