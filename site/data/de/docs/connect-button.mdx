---
title: ConnectButton
description: Verwendung und Anpassung des ConnectButton
---

# ConnectButton

## Verwendung und Anpassung des ConnectButton

Dies ist die Hauptkomponente. Sie ist verantwortlich für das Rendern des Verbindungs-/Trennungsknopfes sowie der Kettenwechsel-UI.

```tsx
import { ConnectButton } from '@rainbow-me/rainbowkit';

export const YourApp = () => {
  return <ConnectButton />;
};
```

> Hinweis: Stellen Sie sicher, dass Ihre App in die erforderlichen Provider eingebunden ist. [Mehr erfahren](/docs/installation#wrap-providers).

### Requisiten

Die `ConnectButton`-Komponente bietet mehrere Requisiten, um ihr Erscheinungsbild anzupassen, einschließlich des Umschaltens der Sichtbarkeit verschiedener Elemente.

<PropsTable
  data={[
    {
      name: 'label',
      required: false,
      default: 'Wallet verbinden',
      type: 'string',
      description: 'Der Text, der auf dem Verbindungs-Button angezeigt wird',
    },
    {
      name: 'accountStatus',
      required: false,
      default: 'vollständig',
      type: '"avatar" | "adresse" | "vollständig" | { smallScreen: AccountStatus, largeScreen?: AccountStatus }',
      typeSimple: 'enum',
      description:
        "Ob das Avatar und/oder die Adresse des aktiven Kontos angezeigt wird",
    },
    {
      name: 'chainStatus',
      required: false,
      default: '{ smallScreen: "icon", largeScreen: "vollständig" }',
      type: '"icon" | "name" | "vollständig" | "keine" | { smallScreen: ChainStatus, largeScreen?: ChainStatus }',
      typeSimple: 'enum',
      description:
        "Ob das Symbol und/oder der Name der aktuellen Kette angezeigt oder vollständig versteckt wird",
    },
    {
      name: 'showBalance',
      required: false,
      default: '{ smallScreen: false, largeScreen: true }',
      type: 'boolean | { smallScreen: boolean, largeScreen?: boolean }',
      typeSimple: 'enum',
      description:
        'Ob das Guthaben neben dem Kontonamen sichtbar ist',
    },
  ]}
/>

> Hinweis: Der eingebaute `largeScreen`-Breakpunkt ist `768px`.

### Beispiele

#### Beschriftung

Verwenden Sie die `label`-Requisite, um einen benutzerdefinierten Text für den `ConnectButton` festzulegen.

```tsx
<ConnectButton label="__Sign in__" />
```

#### Kontostatus

Hier sind einige verschiedene Möglichkeiten, wie Sie die Requisite `accountStatus` verwenden können.

Nur das Avatar des Kontos anzeigen.

```tsx
<ConnectButton accountStatus="__avatar__" />
```

Nur die Adresse des Kontos anzeigen.

```tsx
<ConnectButton accountStatus="__address__" />
```

#### Kettenstatus

Hier sind einige verschiedene Möglichkeiten, wie Sie die Requisite `chainStatus` verwenden können.

Nur das aktuelle Ketten-Icon anzeigen.

```tsx
<ConnectButton chainStatus="__icon__" />
```

Nur den Namen der aktuellen Kette anzeigen.

```tsx
<ConnectButton chainStatus="__name__" />
```

Die Ketten-UI vollständig ausblenden.

```tsx
<ConnectButton chainStatus="__none__" />
```

#### Guthaben anzeigen

Verwenden Sie die Requisite `showBalance`, um das Guthaben auszublenden/anzuzeigen.

Das Guthaben ausblenden.

```tsx
<ConnectButton showBalance={__false__} />
```

#### Responsiv

Diese Requisiten können auch in einem responsiven Format definiert werden.

Auf kleinen Bildschirmen nur das Konto-Icon anzeigen. Aber auf großen Bildschirmen Icon und Adresse anzeigen.

```tsx
<ConnectButton
  accountStatus={{
    smallScreen: 'avatar',
    largeScreen: 'full',
  }}
/>
```

Auf kleinen Bildschirmen Guthaben ausblenden. Aber auf großen Bildschirmen anzeigen.

```tsx
<ConnectButton
  showBalance={{
    smallScreen: false,
    largeScreen: true,
  }}
/>
```
