---
title: Chế độ làm mát
description: Kích hoạt chế độ làm mát
---

# Chế độ làm mát

## Kích hoạt chế độ làm mát 🌈🌈🌈

Bạn có thể kích hoạt chế độ làm mát qua thuộc tính `coolMode` trên `RainbowKitProvider`.

```tsx
import { RainbowKitProvider } from '@rainbow-me/rainbowkit';

const App = () => (
  <RainbowKitProvider coolMode {...etc}>
    {/* Your App */}
  </RainbowKitProvider>
);
```

### Nhưng nó là gì?

Hiệu ứng nổ emoji khi chọn ví từ danh sách ví 🌈🌈🌈. Nó sử dụng biểu tượng của ví bạn chọn.

#### Bản Demo

Nhấn các nút bên dưới để xem chế độ làm mát hoạt động như thế nào.

<CoolMode />
