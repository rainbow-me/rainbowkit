---
title: Billeteras personalizadas
description: Crear una billetera personalizada
---

# Billeteras personalizadas

## Crear una billetera personalizada

> Nota: Esta API es inestable y es probable que cambie en un futuro cercano. Agregaremos más billeteras incorporadas con el tiempo. [Háganos saber](https://github.com/rainbow-me/rainbowkit/issues) si hay alguna billetera particular que le interese.

El tipo de función `Wallet` se proporciona para ayudarlo a definir sus propias billeteras personalizadas. Las siguientes propiedades se pueden configurar en el valor de retorno de su función `Wallet`:

### Propiedades de la Billetera

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: 'Unique ID per wallet',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: 'Human-readable wallet name',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: 'Optional short name for mobile use',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description:
        'URL para el ícono de la billetera o una promesa que resuelve a un URL de datos Base64',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description:
        'Color de acento utilizado en los flujos de descarga para billeteras con una extensión de navegador y una aplicación móvil',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: 'Color de fondo mientras se carga el ícono de la billetera',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          Si la billetera se sabe que está instalada, o{' '}
          <code>undefined</code> si es indeterminada
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: 'Objeto que contiene URLs de descarga',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description:
        'Función para calcular si esta billetera debe ocultarse de la lista. Esto es útil para definir billeteras genéricas de respaldo, por ejemplo, el respaldo integrado de "Injected Wallet" hace uso de esta función',
    },
    {
      name: 'createConnector',
      required: true,
      type: '() => RainbowKitConnector',
      typeSimple: 'RainbowKitConnector',
      description:
        'Función para proporcionar la instancia del conector y la configuración para diferentes métodos de conexión, descritos a continuación',
    },
  ]}
/>

### Propiedades de DownloadUrls

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'Google Play URL',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'Apple App Store URL',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        'Landing page for mobile users when `android` or `ios` are unavailable',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        'Landing page for users that scan the mobile download QR Code',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'Chrome Web Store URL',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'Microsoft Edge Add-ons URL',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'Firefox Browser Add-ons URL',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'Opera add-ons URL',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'Mac App Store URL',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        'Landing page for desktop extension users when browser-compatible URLs are unavailable',
    },
  ]}
/>

### Propiedades de RainbowKitConnector

Las siguientes propiedades se definen en el valor de retorno de la función `createConnector`.

<PropsTable
  data={[
    {
      name: 'mobile',
      required: false,
      type: '{ getUri?: () => Promesa<string> }',
      typeSimple: 'objeto',
      description:
        'Función para resolver una URI de conexión de billetera móvil',
    },
    {
      name: 'escritorio',
      required: false,
      type: '{ getUri?: () => Promesa<string> }',
      typeSimple: 'objeto',
      description: 'Función para resolver un enlace profundo de una aplicación de escritorio',
    },
    {
      name: 'qrCode',
      required: false,
      type: "{ getUri: () => Promesa<string>, instrucciones?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }}}}",
      typeSimple: 'objeto',
      description:
        'Objeto que contiene una función para resolver la URI del código QR, más instrucciones opcionales para la configuración de la billetera móvil',
    },
    {
      name: 'extensión',
      required: false,
      type: "{ instrucciones?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }}}}",
      typeSimple: 'objeto',
      description:
        'Objeto que contiene instrucciones opcionales para la configuración de la extensión del navegador',
    },
  ]}
/>

### Ejemplos

Por ejemplo, para crear una billetera personalizada utilizando WalletConnect:

```tsx
import {
  Chain,
  Wallet,
  getWalletConnectConnector,
} from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
  chains: Chain[];
}

export const rainbow = ({
  chains,
  projectId,
}: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  createConnector: () => {
    const connector = getWalletConnectConnector({ projectId, chains });

    return {
      connector,
      mobile: {
        getUri: async () => {
          const provider = await connector.getProvider();
          const uri = await new Promise<string>(resolve =>
            provider.once('display_uri', resolve)
          );
          return uri;
        },
      },
      qrCode: {
        getUri: async () => {
          const provider = await connector.getProvider();
          const uri = await new Promise<string>(resolve =>
            provider.once('display_uri', resolve)
          );
          return uri;
        },
        instructions: {
          learnMoreUrl: 'https://my-wallet/learn-more',
          steps: [
            {
              description:
                'We recommend putting My Wallet on your home screen for faster access to your wallet.',
              step: 'install',
              title: 'Open the My Wallet app',
            },
            {
              description:
                'After you scan, a connection prompt will appear for you to connect your wallet.',
              step: 'scan',
              title: 'Tap the scan button',
            },
          ],
        },
      },
      extension: {
        instructions: {
          learnMoreUrl: 'https://my-wallet/learn-more',
          steps: [
            {
              description:
                'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
              step: 'install',
              title: 'Install the My Wallet extension',
            },
            {
              description:
                'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
              step: 'create',
              title: 'Create or Import a Wallet',
            },
            {
              description:
                'Once you set up your wallet, click below to refresh the browser and load up the extension.',
              step: 'refresh',
              title: 'Refresh your browser',
            },
          ],
        },
      },
    };
  },
});
```

Luego puedes agregar tu billetera personalizada en la [lista de billeteras](/docs/custom-wallet-list).
