---
title: Carteras Personalizadas
description: Crear una cartera personalizada
---

# Carteras Personalizadas

## Crear una cartera personalizada

> Nota: Esta API es inestable y probablemente cambie en el futuro cercano. Iremos añadiendo más carteras integradas con el tiempo. [Háganos saber](https://github.com/rainbow-me/rainbowkit/issues) si hay alguna cartera en particular que le interese.

El tipo de función `Wallet` se proporciona para ayudarle a definir sus propias carteras personalizadas. Las siguientes propiedades se pueden configurar en el valor de retorno de su función `Wallet`:

### Propiedades de la Cartera

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: 'ID único por cartera',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: 'Nombre de cartera legible por humanos',
    },
    {
      name: 'rdns',
      required: false,
      type: 'string',
      description: 'RDNS para cartera que soporta EIP6963',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: 'Nombre corto opcional para uso móvil',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description:
        'URL para el icono de la cartera, o una promesa que resuelve a un URL de datos Base64',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description:
        'Color de énfasis utilizado en flujos de descarga para carteras con una extensión de navegador y una aplicación móvil',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: 'Color de fondo mientras carga el icono de la cartera',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: 'Si se sabe que la cartera está instalada o <code>undefined</code> si no se puede determinar',
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: 'Objeto que contiene URLs de descarga',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description:
        'Función para calcular si esta cartera debe ocultarse de la lista. Esto es útil para definir carteras genéricas de respaldo, por ejemplo, el "Injected Wallet" integrado utiliza esta función',
    },
    {
      name: 'createConnector',
      required: true,
      type: '() => RainbowKitConnector',
      typeSimple: 'RainbowKitConnector',
      description:
        'Función para proporcionar la instancia del conector y la configuración de diferentes métodos de conexión, descritos a continuación',
    },
    {
      name: 'mobile',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description:
        'Función para resolver un URI de conexión de cartera móvil',
    },
    {
      name: 'desktop',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description: 'Función para resolver un enlace profundo de aplicación de escritorio',
    },
    {
      name: 'qrCode',
      required: false,
      type: "{ getUri: (uri: string) => string, instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Objeto que contiene una función para resolver el URI del código QR, además de instrucciones opcionales de configuración de cartera móvil',
    },
    {
      name: 'extension',
      required: false,
      type: "{ instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Objeto que contiene instrucciones opcionales de configuración de extensiones de navegador',
    },
    {
      name: 'createConnector',
      required: true,
      type: "createConnector: (details: WalletDetailsParams) => CreateConnectorFn",
      typeSimple: '(details: WalletDetailsParams) => CreateConnectorFn',
      description: 'Función para proporcionar la instancia del conector',
    },
  ]}
/>

### Propiedades de DownloadUrls

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'URL de Google Play',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'URL de la App Store de Apple',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        'Página de destino para usuarios móviles cuando `android` o `ios` no están disponibles',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        'Página de destino para usuarios que escanean el código QR de descarga móvil',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'URL de la Chrome Web Store',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'URL de Microsoft Edge Add-ons',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'URL de Firefox Browser Add-ons',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'URL de complementos de Opera',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'URL de la Mac App Store',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        'Página de destino para usuarios de extensiones de escritorio cuando no hay URLs compatibles con navegadores disponibles',
    },
  ]}
/>

### Ejemplos

Por ejemplo, para crear una cartera personalizada usando WalletConnect:

```tsx
import { Wallet, getWalletConnectConnector } from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
}

export const rainbow = ({ projectId }: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  mobile: {
    getUri: (uri: string) => uri,
  },
  qrCode: {
    getUri: (uri: string) => uri,
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend putting My Wallet on your home screen for faster access to your wallet.',
          step: 'install',
          title: 'Open the My Wallet app',
        },
        {
          description:
            'After you scan, a connection prompt will appear for you to connect your wallet.',
          step: 'scan',
          title: 'Tap the scan button',
        },
      ],
    },
  },
  extension: {
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
          step: 'install',
          title: 'Install the My Wallet extension',
        },
        {
          description:
            'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
          step: 'create',
          title: 'Create or Import a Wallet',
        },
        {
          description:
            'Once you set up your wallet, click below to refresh the browser and load up the extension.',
          step: 'refresh',
          title: 'Refresh your browser',
        },
      ],
    },
  },
  createConnector: getWalletConnectConnector({ projectId }),
});
```

Luego puede añadir su cartera personalizada en la [lista de carteras](/docs/custom-wallet-list).
