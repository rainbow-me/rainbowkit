---
title: 自訂 ConnectButton
description: 建立自訂 ConnectButton
---

# 自訂 ConnectButton

## 建立自訂 ConnectButton

您可以使用低層次的 `ConnectButton.Custom` 來創建自訂的連接按鈕。此組件會呈現一個函數，內含重新實作內建按鈕所需的一切。

內建按鈕的極簡重新實作可能類似以下這樣：

```tsx
import { ConnectButton } from '@rainbow-me/rainbowkit';

export const YourApp = () => {
  return (
    <ConnectButton.Custom>
      {({
        account,
        chain,
        openAccountModal,
        openChainModal,
        openConnectModal,
        authenticationStatus,
        mounted,
      }) => {
        // Note: If your app doesn't use authentication, you
        // can remove all 'authenticationStatus' checks
        const ready = mounted && authenticationStatus !== 'loading';
        const connected =
          ready &&
          account &&
          chain &&
          (!authenticationStatus ||
            authenticationStatus === 'authenticated');

        return (
          <div
            {...(!ready && {
              'aria-hidden': true,
              'style': {
                opacity: 0,
                pointerEvents: 'none',
                userSelect: 'none',
              },
            })}
          >
            {(() => {
              if (!connected) {
                return (
                  <button onClick={openConnectModal} type="button">
                    Connect Wallet
                  </button>
                );
              }

              if (chain.unsupported) {
                return (
                  <button onClick={openChainModal} type="button">
                    Wrong network
                  </button>
                );
              }

              return (
                <div style={{ display: 'flex', gap: 12 }}>
                  <button
                    onClick={openChainModal}
                    style={{ display: 'flex', alignItems: 'center' }}
                    type="button"
                  >
                    {chain.hasIcon && (
                      <div
                        style={{
                          background: chain.iconBackground,
                          width: 12,
                          height: 12,
                          borderRadius: 999,
                          overflow: 'hidden',
                          marginRight: 4,
                        }}
                      >
                        {chain.iconUrl && (
                          <img
                            alt={chain.name ?? 'Chain icon'}
                            src={chain.iconUrl}
                            style={{ width: 12, height: 12 }}
                          />
                        )}
                      </div>
                    )}
                    {chain.name}
                  </button>

                  <button onClick={openAccountModal} type="button">
                    {account.displayName}
                    {account.displayBalance
                      ? ` (${account.displayBalance})`
                      : ''}
                  </button>
                </div>
              );
            })()}
          </div>
        );
      }}
    </ConnectButton.Custom>
  );
};
```

以下道具將傳遞至您的渲染函數。

### 帳號屬性

<PropsTable
  data={[
    {
      name: 'account',
      required: false,
      type: 'object | undefined',
      description:
        '包含當前帳號詳情的物件，如下所述',
    },
    {
      name: 'account.address',
      required: false,
      type: 'string',
      description: (
        <>
          完整帳號地址，例如
          "0x7a3d05c70581bD345fe117c06e45f9669205384f"
        </>
      ),
    },
    {
      name: 'account.balanceDecimals',
      required: false,
      type: 'string | undefined	',
      description: '帳號餘額的小數格式',
    },
    {
      name: 'account.balanceFormatted',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          已格式化為字串的帳號餘額，例如{' '}
          <code>1.234567890123456789</code>
        </>
      ),
    },
    {
      name: 'account.balanceSymbol',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          餘額的貨幣符號，例如 <code>ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayBalance',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          餘額格式化為 3 個有效數字，加上符號，例如 <code>1.23 ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayName',
      required: false,
      type: 'string',
      description: (
        <>
          ENS 名稱或地址截斷版本，例如{' '}
          <code>"rainbowwallet.eth"</code> 或 <code>"0x7a…384f"</code>
        </>
      ),
    },
    {
      name: 'account.ensAvatar',
      required: false,
      type: 'string	| undefined',
      description: 'ENS Avatar URI',
    },
    {
      name: 'account.ensName',
      required: false,
      type: 'string	| undefined',
      description: (
        <>
          ENS 名稱，例如 <code>rainbowwallet.eth</code>
        </>
      ),
    },
    {
      name: 'account.hasPendingTransactions',
      required: false,
      type: 'boolean',
      description:
        '布林值指示當前鏈上帳號是否有待處理交易',
    },
  ]}
/>

### 鏈屬性

<PropsTable
  data={[
    {
      name: 'chain',
      required: false,
      type: 'object | undefined',
      description:
        '包含當前鏈詳情的物件，如下所述',
    },
    {
      name: 'chain.hasIcon',
      required: false,
      type: 'boolean',
      description: '鏈是否指定了圖標',
    },
    {
      name: 'chain.iconUrl',
      required: false,
      type: 'string | undefined',
      description:
        '鏈圖標 URL（下載 Base64 數據 URL 時，也可能未定義）',
    },
    {
      name: 'chain.iconBackground',
      required: false,
      type: 'string | undefined',
      description:
        '圖像加載時顯示的鏈圖標背景',
    },
    {
      name: 'chain.id',
      required: false,
      type: 'number',
      description: (
        <>
          鏈 ID，例如 <code>1</code>
        </>
      ),
    },
    {
      name: 'chain.name',
      required: false,
      type: 'string | undefined',
      description: (
        <>
          鏈名稱，例如 <code>"以太坊"</code>
        </>
      ),
    },
    {
      name: 'chain.unsupported',
      required: false,
      type: 'boolean | undefined',
      description:
        '布林值指示當前鏈是否不受支持',
    },
  ]}
/>

### 彈窗狀態屬性

<PropsTable
  data={[
    {
      name: 'openAccountModal',
      required: false,
      type: '() => void',
      description: '開啟帳號彈窗的函數',
    },
    {
      name: 'openChainModal',
      required: false,
      type: '() => void',
      description: '開啟鏈彈窗的函數',
    },
    {
      name: 'openConnectModal',
      required: false,
      type: '() => void',
      description: '開啟連接彈窗的函數',
    },
    {
      name: 'accountModalOpen',
      required: false,
      type: 'boolean',
      description:
        '布林值指示帳號彈窗是否開啟',
    },
    {
      name: 'chainModalOpen',
      required: false,
      type: 'boolean',
      description: '布林值指示鏈彈窗是否開啟',
    },
    {
      name: 'connectModalOpen',
      required: false,
      type: 'boolean',
      description:
        '布林值指示連接彈窗是否開啟',
    },
  ]}
/>

### 一般狀態屬性

<PropsTable
  data={[
    {
      name: 'mounted',
      required: false,
      type: 'boolean',
      description:
        '布林值指示組件是否已掛載',
    },
    {
      name: 'authenticationStatus',
      required: false,
      type: '"loading" | "unauthenticated" | "authenticated" | undefined',
      description:
        '驗證狀態，或在未設定驗證時為 undefined',
    },
  ]}
/>
