---
title: RPC Providers
description: Customizing RPC providers
---

# RPC Providers

## Customizing RPC providers

RainbowKit has built-in RPC provider support so you don't have to worry about defining RPC URLs & a provider instance.

RainbowKit exports `rpcProvider` that you can use to instantiate your RPC provider.

#### Alchemy

To use the built-in Alchemy RPC provider, you will need to pass through your **chains** & **Alchemy ID**.

```tsx line=1,4-7,11,17
import { rpcProvider, getDefaultWallets } from '@rainbow-me/rainbowkit';
import { chain } from 'wagmi';

const { chains, provider } = rpcProvider.alchemy(
  [chain.mainnet, chain.polygon], 
  process.env.ALCHEMY_ID
);

const { connectors } = getDefaultWallets({
  appName: 'RainbowKit demo',
  chains
});

const wagmiClient = createClient({
  autoConnect: true,
  connectors,
  provider,
});

...
```

If you wish to use a WebSocket provider, the RPC Provider also returns a `webSocketProvider`.

```tsx line=4,18
import { rpcProvider, getDefaultWallets } from '@rainbow-me/rainbowkit';
import { chain } from 'wagmi';

const { chains, provider, webSocketProvider } = rpcProvider.alchemy(
  [chain.mainnet, chain.polygon], 
  process.env.ALCHEMY_ID
);

const { connectors } = getDefaultWallets({
  appName: 'RainbowKit demo',
  chains
});

const wagmiClient = createClient({
  autoConnect: true,
  connectors,
  provider,
  webSocketProvider,
});

...
```

#### Infura

To use the built-in Infura RPC provider, you will need to pass through your **chains** & **Infura ID**.

```tsx line=1,4-7,11,17
import { rpcProvider, getDefaultWallets } from '@rainbow-me/rainbowkit';
import { chain } from 'wagmi';

const { chains, provider } = rpcProvider.infura(
  [chain.mainnet, chain.polygon],
  process.env.INFURA_ID,
);

const { connectors } = getDefaultWallets({
  appName: 'RainbowKit demo',
  chains,
});

const wagmiClient = createClient({
  autoConnect: true,
  connectors,
  provider,
});

...
```

If you wish to use a WebSocket provider, the RPC Provider also returns a `webSocketProvider`.

```tsx line=4,18
import { rpcProvider, getDefaultWallets } from '@rainbow-me/rainbowkit';
import { chain } from 'wagmi';

const { chains, provider, webSocketProvider } = rpcProvider.infura(
  [chain.mainnet, chain.polygon], 
  process.env.INFURA_ID
);

const { connectors } = getDefaultWallets({
  appName: 'RainbowKit demo',
  chains
});

const wagmiClient = createClient({
  autoConnect: true,
  connectors,
  provider,
  webSocketProvider,
});

...
```

#### Custom

To use a custom RPC provider, you will need to pass through your **chains** and a map of **RPC URLs** (`rpcUrls`) of the chains you want to support.

```tsx line=1,4-12,16,22
import { rpcProvider, getDefaultWallets } from '@rainbow-me/rainbowkit';
import { chain } from 'wagmi';

const { chains, provider } = rpcProvider.custom(
  [chain.mainnet, chain.polygon],
  {
    rpcUrls: {
      [chain.mainnet.id]: 'https://mainnet.example.com',
      [chain.polygon.id]: 'https://polygon.example.com',
    }
  }
);

const { connectors } = getDefaultWallets({
  appName: 'RainbowKit demo',
  chains,
});

const wagmiClient = createClient({
  autoConnect: true,
  connectors,
  provider,
});

...
```

If you want to use a WebSocket provider, you can pass through your WebSocket RPC URLs using the `webSocketRpcUrls` option.

```tsx line=4,11-14,27
import { rpcProvider, getDefaultWallets } from '@rainbow-me/rainbowkit';
import { chain } from 'wagmi';

const { chains, provider, webSocketProvider } = rpcProvider.custom(
  [chain.mainnet, chain.polygon],
  {
    rpcUrls: {
      [chain.mainnet.id]: 'https://mainnet.example.com',
      [chain.polygon.id]: 'https://polygon.example.com',
    },
    webSocketRpcUrls: {
      [chain.mainnet.id]: 'wss://mainnet.example.com',
      [chain.polygon.id]: 'wss://polygon.example.com',
    }
  }
);

const { connectors } = getDefaultWallets({
  appName: 'RainbowKit demo',
  chains,
});

const wagmiClient = createClient({
  autoConnect: true,
  connectors,
  provider,
  webSocketProvider
});

...
```