---
title: Carteiras Personalizadas
description: Crie uma carteira personalizada
---

# Carteiras Personalizadas

## Crie uma carteira personalizada

> Nota: Esta API está instável e provavelmente mudará em breve. Estaremos adicionando mais carteiras embutidas ao longo do tempo. [Informe-nos](https://github.com/rainbow-me/rainbowkit/issues) se houver carteiras específicas do seu interesse.

O tipo de função `Wallet` é fornecido para ajudá-lo a definir suas próprias carteiras personalizadas. As seguintes propriedades podem ser configuradas no valor de retorno de sua função `Wallet`:

### Propriedades da Carteira

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: 'ID único por carteira',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: 'Nome legível para humanos da carteira',
    },
    {
      name: 'rdns',
      required: false,
      type: 'string',
      description: 'RDNS para carteira que suporta EIP6963',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: 'Nome curto opcional para uso móvel',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description:
        'URL para ícone da carteira, ou uma promessa que se resolve para um URL de dados Base64',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description:
        'Cor de destaque usada nos fluxos de download para carteiras com extensão de navegador e aplicativo móvel',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: 'Cor de fundo enquanto o ícone da carteira carrega',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          Se a carteira é conhecida por estar instalada, ou{' '}
          <code>undefined</code> se for indeterminado
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: 'Objeto contendo URLs de download',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description:
        'Função para calcular se esta carteira deve ser ocultada da lista. Isso é útil para definir carteiras de fallback genéricas, por exemplo, o fallback "Injected Wallet" embutido utiliza este recurso',
    },
    {
      name: 'createConnector',
      required: true,
      type: '() => RainbowKitConnector',
      typeSimple: 'RainbowKitConnector',
      description:
        'Função para fornecer a instância do conector e a configuração para diferentes métodos de conexão, descritos abaixo',
    },
    {
      name: 'mobile',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description:
        'Função para resolver um URI de conexão de carteira móvel',
    },
    {
      name: 'desktop',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description: 'Função para resolver um link profundo de aplicativo de desktop',
    },
    {
      name: 'qrCode',
      required: false,
      type: "{ getUri: (uri: string) => string, instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Objeto contendo uma função para resolver o URI do código QR, além de instruções opcionais de configuração de carteira móvel',
    },
    {
      name: 'extension',
      required: false,
      type: "{ instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Objeto contendo instruções opcionais para configuração de extensão de navegador',
    },
    {
      name: 'createConnector',
      required: true,
      type: "createConnector: (details: WalletDetailsParams) => CreateConnectorFn",
      typeSimple: '(details: WalletDetailsParams) => CreateConnectorFn',
      description: 'Função para fornecer a instância do conector',
    },
  ]}
/>

### Propriedades de DownloadUrls

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'URL do Google Play',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'URL da App Store da Apple',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        'Página de destino para usuários móveis quando `android` ou `ios` não estão disponíveis',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        'Página de destino para usuários que escaneiam o Código QR de download móvel',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'URL da Chrome Web Store',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'URL de complementos do Microsoft Edge',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'URL de Complementos do Firefox',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'URL de complementos do Opera',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'URL da Mac App Store',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        'Página de destino para usuários de extensão de navegador quando URLs compatíveis com o navegador não estão disponíveis',
    },
  ]}
/>

### Exemplos

Por exemplo, para criar uma carteira personalizada usando o WalletConnect:

```tsx
import { Wallet, getWalletConnectConnector } from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
}

export const rainbow = ({ projectId }: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  mobile: {
    getUri: (uri: string) => uri,
  },
  qrCode: {
    getUri: (uri: string) => uri,
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend putting My Wallet on your home screen for faster access to your wallet.',
          step: 'install',
          title: 'Open the My Wallet app',
        },
        {
          description:
            'After you scan, a connection prompt will appear for you to connect your wallet.',
          step: 'scan',
          title: 'Tap the scan button',
        },
      ],
    },
  },
  extension: {
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
          step: 'install',
          title: 'Install the My Wallet extension',
        },
        {
          description:
            'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
          step: 'create',
          title: 'Create or Import a Wallet',
        },
        {
          description:
            'Once you set up your wallet, click below to refresh the browser and load up the extension.',
          step: 'refresh',
          title: 'Refresh your browser',
        },
      ],
    },
  },
  createConnector: getWalletConnectConnector({ projectId }),
});
```

Você pode então adicionar sua carteira personalizada na [lista de carteiras](/docs/custom-wallet-list).
