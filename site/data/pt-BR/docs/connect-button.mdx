---
title: BotãoConectar
description: Usando e personalizando o BotãoConectar
---

# BotãoConectar

## Usando e personalizando o BotãoConectar

Este é o componente principal. Ele é responsável por renderizar o botão de conectar/desconectar, bem como a interface do usuário para troca de cadeia.

```tsx
import { ConnectButton } from '@rainbow-me/rainbowkit';

export const YourApp = () => {
  return <ConnectButton />;
};
```

> Nota: Certifique-se de que seu aplicativo esteja envolvido nos provedores necessários. [Leia mais](/docs/installation#wrap-providers).

### Propriedades

O componente `BotãoConectar` expõe várias props para personalizar sua aparência, incluindo a alternância da visibilidade de diferentes elementos.

<PropsTable
  data={[
    {
      name: 'label',
      required: false,
      default: 'Conectar Carteira',
      type: 'string',
      description: 'O texto exibido no botão Conectar',
    },
    {
      name: 'accountStatus',
      required: false,
      default: 'completo',
      type: '"avatar" | "endereço" | "completo" | { smallScreen: AccountStatus, largeScreen?: AccountStatus }',
      typeSimple: 'enum',
      description:
        "Se o avatar e/ou o endereço da conta ativa são exibidos",
    },
    {
      name: 'chainStatus',
      required: false,
      default: '{ smallScreen: "icon", largeScreen: "full" }',
      type: '"icon" | "nome" | "completo" | "nenhum" | { smallScreen: ChainStatus, largeScreen?: ChainStatus }',
      typeSimple: 'enum',
      description:
        "Se o ícone e/ou o nome da cadeia atual são exibidos ou totalmente ocultos",
    },
    {
      name: 'showBalance',
      required: false,
      default: '{ smallScreen: false, largeScreen: true }',
      type: 'boolean | { smallScreen: boolean, largeScreen?: boolean }',
      typeSimple: 'enum',
      description:
        'Se o saldo é visível ao lado do nome da conta',
    },
  ]}
/>

> Nota: o ponto de interrupção `largeScreen` incorporado é `768px`.

### Exemplos

#### Etiqueta

Use a propriedade `label` para definir um texto personalizado para o `ConnectButton`.

```tsx
<ConnectButton label="__Sign in__" />
```

#### Status da conta

Aqui estão algumas maneiras diferentes de usar a propriedade `accountStatus`.

Mostre apenas o avatar da conta.

```tsx
<ConnectButton accountStatus="__avatar__" />
```

Mostre apenas o endereço da conta.

```tsx
<ConnectButton accountStatus="__address__" />
```

#### Status da cadeia

Aqui estão algumas maneiras diferentes de usar a propriedade `chainStatus`.

Mostre apenas o ícone da cadeia atual.

```tsx
<ConnectButton chainStatus="__icon__" />
```

Mostre apenas o nome da cadeia atual.

```tsx
<ConnectButton chainStatus="__name__" />
```

Oculte totalmente a interface da cadeia.

```tsx
<ConnectButton chainStatus="__none__" />
```

#### Mostrar saldo

Use a propriedade `showBalance` para ocultar/mostrar o saldo.

Ocultar o saldo.

```tsx
<ConnectButton showBalance={__false__} />
```

#### Responsivo

Essas propriedades também podem ser definidas em um formato responsivo.

Em telas pequenas, mostre somente o ícone da conta. Mas em telas grandes, mostre o ícone e o endereço.

```tsx
<ConnectButton
  accountStatus={{
    smallScreen: 'avatar',
    largeScreen: 'full',
  }}
/>
```

Em telas pequenas, oculte o saldo. Mas em telas grandes, mostre-o.

```tsx
<ConnectButton
  showBalance={{
    smallScreen: false,
    largeScreen: true,
  }}
/>
```
