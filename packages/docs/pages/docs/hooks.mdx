import { History } from '../../components/history'

# Hooks

RainbowKit provides a variety of hooks that makes it easy to access data and perform actions within your dapp.

## useTxHistory

Fetch transaction history for an etherium `address` with a given `provider` and `fetcher` function with any`options` needed.

```tsx
import React from 'react'
import { useTxHistory, logsFetcher } from '@rainbowkit/hooks'
import { useWeb3React } from '@web3-react/core'
import { setupProvider } from '@rainbowkit/utils'
import { EtherscanProvider, TransactionResponse } from '@ethersproject/providers'
import { formatEther } from '@ethersproject/units'

const provider = new EtherscanProvider()

const HistoryExample = () => {
  const { account: address } = useWeb3React()

  const {
    data: txes,
    loading,
    error
  } = useTxHistory<TransactionResponse>({
    address,
    fetcher: etherscanFetcher,
    provider,
    options: {}
  })

  if (error) return <strong style={{ color: 'red' }}>Error! {error?.message}</strong>

  if (data && !loading)
    return (
      <>
        {txes.slice(0, 10).map((tx) => (
          <div key={tx.hash} style={{ fontFamily: 'monospace' }}>
            <a href={`https://etherscan.io/tx/${tx.hash}`}>{tx.hash.slice(0, 8)}</a>
            {' | '}
            <span>Value: {parseFloat(formatEther(tx.value)).toPrecision(3)} ETH</span>
          </div>
        ))}
      </>
    )

  if (loading) return <p>Loading...</p>
}
```

The optional `fetcher` function should take the following form:

```ts
export const etherscanFetcher: TxHistoryFetcher<TransactionResponse, EtherscanProvider> = async ({
  provider,
  address,
  options = {}
}) => {
  // Return an array of transactions.
  return provider.getHistory(address, options.fromBlock, options.toBlock)
}
```

### Example

<History />

## useConnector

A React hook for using individual connectors from web3-react. Pass a web3-react `connector` and a boolean indicating if web3-react should `connectOnMount` with the given connector. You can pass an optional `storageProvider` which defaults to `window.localStorage`.

```tsx
import { Connector, ConnectorProps } from 'my-custom-connector'
import { useConnector } from '@rainbowkit/hooks'

export type CustomWalletOptions = ConnectorProps & { connectOnMount?: boolean }

export const useCustomConnector = (opts?: CustomWalletOptions) => {
  const options = opts || {}
  const { connectOnMount, ...customConnectorOpts } = options

  const connector = new Connector(customConnectorOpts)

  const {
    provider,
    connect,
    isConnected,
    disconnect,
    chainId,
    connector,
    address,
    error
  } = useConnector(connector, connectOnMount, window.localStorage)

  return {
    provider,
    connect,
    isConnected,
    disconnect,
    chainId,
    connector,
    address,
    error
  }
}
```

## useConnectOnMount

A React hook that attempts to connect to a provider as if it was initialized before.

// TODO Unclear when this would be used instead of useConnector

```tsx
import { useState, useEffect } from 'react'
import { useWeb3React } from '@web3-react/core'
import { useConnectOnMount } from '@rainbowkit/hooks'

const App = () => {
  const { library: provider, connector } = useWeb3React()

  const [storage, set] = useState<Storage>()

  useEffect(() => {
    set(sessionStorage)
  }, [])

  useConnectOnMount(connector, true, storage)

  return <h1>hello world</h1>
}
```
